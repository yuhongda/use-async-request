import { useRequest } from './useAsyncRequest'
import type {
  RequestFunction,
  TransformFunction,
  UseRequestOptions,
  UseRequestResults,
  UseRequestAction
} from './useAsyncRequest'

export type {
  RequestFunction,
  TransformFunction,
  UseRequestOptions,
  UseRequestResults,
  UseRequestAction
}
export { useRequest }
export default {
  useRequest
}
