import { useRequest } from './useRequest'
import type {
  RequestFunction,
  TransformFunction,
  UseRequestOptions,
  UseRequestResults,
  UseRequestAction
} from './useRequest'

export type {
  RequestFunction,
  TransformFunction,
  UseRequestOptions,
  UseRequestResults,
  UseRequestAction
}
export { useRequest }
export default {
  useRequest
}
