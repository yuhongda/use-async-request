const defaultValue = {
  data: null,
  loading: false,
  error: null
};
const useRequest = (options) => {
  return defaultValue;
};

var index = {
  useRequest
};

export { index as default, useRequest };
